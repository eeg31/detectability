
#priors: should be a named list of prior probabilities (i.e. endemic syndrome incidences)
#ll_data should be a data frame with columns named after symptoms and one row per case

test_countries_EVD <- function(x='GH', y='SL'){

  #load some (fake) data on Ebola

  #compare likelihood of observing this outbreak in country x vs country y

}
